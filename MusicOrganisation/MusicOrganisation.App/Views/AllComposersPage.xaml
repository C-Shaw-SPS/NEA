<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MusicOrganisation.Lib.ViewModels;assembly=MusicOrganisation.Lib"
             x:Class="MusicOrganisation.App.Views.AllComposersPage"
             Title="Composers">

    <ContentPage.BindingContext>
        <viewmodels:AllComposersViewModel/>
    </ContentPage.BindingContext>
    
    <VerticalStackLayout>

        <Grid RowDefinitions="500,*">
            <CollectionView ItemsSource="{Binding Composers}"
                        VerticalOptions="FillAndExpand"
                        VerticalScrollBarVisibility="Always">

                <CollectionView.ItemsLayout>

                    <LinearItemsLayout Orientation="Vertical"
                                   ItemSpacing="20"/>

                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>

                    <DataTemplate>
                        <StackLayout>
                            <Label Text="{Binding Name}"/>
                        </StackLayout>
                    </DataTemplate>

                </CollectionView.ItemTemplate>

            </CollectionView>
            
        </Grid>

        <Button Command="{Binding InitialiseCommand}"
                Text="Initialise composers"/>
    </VerticalStackLayout>
</ContentPage>